services:
  errorAndLogService:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app 
    networks:
      - ucomand-network

networks:
  ucomand-network:
    external: true